{
  "buildCommand": "cd apps/web && bunx prisma generate --schema=../../packages/db/prisma/schema.prisma && bun run build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
