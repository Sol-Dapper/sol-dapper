{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "db:generate": "prisma generate",
    "postinstall": "prisma generate",
    "build": "bun run db:generate && bun build index.ts --outdir public --target bun",
    "start": "bun index.ts",
    "dev": "bun --watch index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/cors": "^2.8.19",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20.19.13",
    "typescript": "^5.8.2",
    "prisma": "^6.15.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
