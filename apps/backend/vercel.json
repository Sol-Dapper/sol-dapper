{
  "version": 2,
  "name": "my-backend",
  "buildCommand": "cp -r ../../packages ./packages && pnpm install && pnpm run build",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}