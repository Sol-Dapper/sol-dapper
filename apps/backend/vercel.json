{
  "version": 2,
  "name": "my-backend",
  "buildCommand": "cd apps/backend && bun install && bunx prisma generate --schema=../../packages/db/prisma/schema.prisma && bun run build",
  "devCommand": "cd apps/backend && bun run dev",
  "installCommand": "bun install",
  "framework": null,
  "functions": {
    "apps/backend/dist/index.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/backend/dist/index.js"
    }
  ]
}
