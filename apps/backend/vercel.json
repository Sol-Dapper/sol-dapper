{
  "version": 2,
  "name": "my-backend",
  "buildCommand": "cp -r ../../packages ./packages && bun install && bun run build",
  "devCommand": "bun run dev",
  "installCommand": "bun install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}