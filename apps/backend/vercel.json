{
  "version": 2,
  "name": "backend",
  "buildCommand": "cp -r ../../packages ./packages && bun install && bunx prisma generate --schema=./packages/db/prisma/schema.prisma && bun run build",
  "devCommand": "bun run dev",
  "installCommand": "bun install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}
